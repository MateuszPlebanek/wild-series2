{% extends 'base.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
  <h1>Create a new Category</h1>

  {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Add category</button>
  {{ form_end(form) }}

  <p><a href="{{ path('category_index') }}">← Back to categories</a></p>
{% endblock %}
